numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
joblib==1.5.3
pytesseract==0.3.13
pillow==12.2.0
opencv-python==4.13.0.92
PyMuPDF==1.27.2.3
python-docx==1.2.0

# Added missing dependencies required for Core Bot functionality
google-api-python-client==2.197.0
google-auth-httplib2==0.3.0
google-auth-oauthlib==1.4.0
beautifulsoup4==4.15.0
reportlab==4.4.10
requests==2.34.2
python-whois==0.9.6
python-dotenv==1.2.2
oletools==0.60.2

# Mobile app / 24-7 server backend
fastapi==0.124.0
uvicorn[standard]==0.48.0
pydantic==2.12.5
redis==8.0.0
python-multipart==0.0.29
cryptography==48.0.0
httpx==0.28.1
psycopg[binary]==3.3.2

# Added for QR-code phishing (quishing) detection (Feature 1)
# Note: also requires libzbar0 system package (see Dockerfile)
pyzbar==0.1.9

# Feature 7: Voice/Vishing detection — OPTIONAL
# Uncomment the lines below only when FEATURE_VISHING_DETECTION_ENABLED=true
# and you have enough RAM (whisper tiny ~75MB, base ~150MB).
# On Koyeb/Render free tier (512MB limit) use tiny only.
# openai-whisper==20240930
# torch>=2.0.0          # pulled in automatically by whisper
# ffmpeg-python==0.2.0  # also needs system ffmpeg (see Dockerfile comment)
